import { Location, LocationPostRequest, TransformedLocation, LocationSearchResponse } from "./location";

interface HealthResponse {
    status: string;
}

export { HealthResponse, LocationPostRequest, Location, TransformedLocation, LocationSearchResponse };
